From the summaries you provided, there is **not enough retrieved technical content** to truthfully extract the **actual** OpenFUSIONToolkit/TokaMaker **Python API names**, a confirmed **minimal working script**, or the exact call that **writes EQDSK**.

What *can* be concluded reliably from the summaries is:

- **TokaMaker is the OFT component** intended for **axisymmetric static and time-dependent ideal-MHD equilibria**, i.e. the **Grad–Shafranov** problem in practice.  
- OFT uses a **finite-element framework on unstructured meshes** (2D/3D, variable-order FEM). So the workflow is inherently “define geometry → generate/load mesh → assign regions/BCs/sources → solve → postprocess/output”.
- The GitHub summary explicitly says the concrete details you need (Python imports, class/function names, examples, output writers like EQDSK) are in:
  - the installed documentation: `.../doc/Documentation.html`
  - and/or bundled examples in the install / repo (not included in the retrieved text you summarized)
- The Zenodo record could not be accessed (403), and the arXiv paper content was not actually provided in the retrieved text you summarized—so I can’t extract API names from them.

## Typical TokaMaker workflow (high-confidence, but not API-exact)
Even without the specific API surface, the expected equilibrium workflow in a GS tool like TokaMaker (as suggested by the OFT description) is:

1. **Initialize OFT/TokaMaker session**
2. **Define geometry**
   - limiter/vessel boundary (R–Z polygon)
   - coil set geometry and currents (if vacuum or coil-driven)
3. **Create or load an unstructured FEM mesh**
   - 2D axisymmetric domain mesh of the poloidal cross-section
4. **Assign physics**
   - vacuum case: coil currents + boundary conditions
   - plasma case: specify plasma boundary (LCFS/free-boundary) and profiles/constraints (e.g., \(p(\psi)\), \(F(\psi)\), Ip, beta, q-profile constraints, etc.)
5. **Solve Grad–Shafranov**
6. **Postprocess**
   - flux surfaces, X-point, LCFS, magnetic axis, safety factor, etc.
7. **Write outputs**
   - common equilibrium interchange formats include **EQDSK / g-file**

## Minimal working Python code: what I can provide now
I can only provide **a placeholder skeleton** (not guaranteed to run) because the **module names and function names were not present** in any retrieved summary. Still, this shows the *shape* of what the script will look like once we have the API:

```python
# PSEUDOCODE / SKELETON (API NAMES NOT CONFIRMED)

def build_mesh_and_solve(vacuum=True):
    # 1) import tokamaker bindings (exact module name unknown from summaries)
    import tokamaker  # placeholder

    # 2) initialize
    eq = tokamaker.Equilibrium()  # placeholder

    # 3) geometry / boundary
    # limiter boundary polygon in (R, Z)
    wall_rz = [(1.0, -1.0), (2.0, -1.0), (2.0, 1.0), (1.0, 1.0)]
    eq.set_wall_boundary(wall_rz)  # placeholder

    # 4) mesh
    # either generate mesh or load from file
    eq.generate_mesh(max_area=1e-3)  # placeholder
    # OR: eq.load_mesh("mesh_file")   # placeholder

    # 5) coils / sources
    coils = [
        {"name": "PF1", "center": (2.2, 0.8), "radius": 0.05, "current": 1e5},
    ]
    eq.set_coils(coils)  # placeholder

    if not vacuum:
        # plasma profiles / constraints (placeholders)
        eq.set_plasma_current(1e6)     # Ip
        eq.set_pressure_profile("...") # p(psi)
        eq.set_fpol_profile("...")     # F(psi)

    # 6) solve
    eq.solve()  # placeholder

    # 7) output
    eq.write_eqdsk("out.gEQDSK")  # placeholder


if __name__ == "__main__":
    build_mesh_and_solve(vacuum=True)
```

This is intentionally labeled **pseudocode** because none of the retrieved summaries contain the authoritative symbols (e.g., whether it’s `from OpenFUSIONToolkit import tokamaker`, whether the class is `TokaMaker` vs `Equilibrium`, whether mesh is made via a separate `mesh` module, etc.).

## What I need from you to produce a *real*, runnable minimal script with exact API names
Any one of these is enough:

1. Paste the relevant sections from **`Documentation.html`** (especially:
   - the Python import path(s)
   - TokaMaker class names
   - mesh/geometry API section
   - “write EQDSK / gfile” section)
2. Paste or point to a **TokaMaker Python example** file from the repo/install (even 50–150 lines).
3. Upload the **arXiv PDF text** (2311.07719) or paste the sections describing:
   - code usage
   - I/O formats
   - EQDSK mention (if any)

With that, I can respond with:
- the **exact imports**
- the **minimal working script** (vacuum and plasma variants if you want)
- the exact **EQDSK writer call** and required metadata
- common pitfalls (units, boundary conventions, coil definitions, free- vs fixed-boundary)

## Two quick clarifying questions (so the minimal example matches your target)
1. Do you want **vacuum equilibrium** (coils only) or a **plasma equilibrium** (with profiles/Ip constraints)?
2. Do you already have a **mesh**, or do you want the example to **generate** one from a simple boundary polygon?

If you paste the `Documentation.html` snippet (even just the “TokaMaker Python” section and anything mentioning “eqdsk”), I’ll turn this into a concrete, runnable minimal script with the real API names.