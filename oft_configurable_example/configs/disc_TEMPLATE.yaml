# TEMPLATE: discretization-only variant for the config-driven OFT/TokaMaker example
#
# How to use:
#   1) Copy discretization_config.yaml (baseline) to a new file under ./configs/
#   2) Apply changes primarily under:
#        - oft_case.mesh.dx
#        - oft_case.discretization.fe_order
#        - oft_case.discretization.full_domain (if desired)
#   3) Keep physics fixed for a discretization sweep:
#        - oft_case.model.type: gs_vacuum
#        - oft_case.physics.vacuum_bc.psi: 0.0
#        - same geometry boundary points
#   4) Validate + run:
#        python validate_config.py --config <thisfile> --effective /tmp/effective.yaml
#        python run_equilibrium.py --config <thisfile> --output-dir outputs --force
#
# NOTE: This file is a template and may not validate until you fill all required keys.
#       Start from discretization_config.yaml and edit from there.
